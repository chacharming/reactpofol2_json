import React from 'react';
import aboutcss from '../css/about.module.css';

function About(props) {
    return (
        <div className={`${aboutcss.introduce}`}>
            
        </div>
    );
}

export default About;